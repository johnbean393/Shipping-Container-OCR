{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Container IDs",
  "description": "Schema for an array of container IDs extracted from images.",
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "object",
    "title": "Container",
    "description": "Container ID information.",
    "required": ["container_id"],
    "properties": {
      "container_id": {
        "type": "string",
        "description": "Unique identifier for the container, e.g., 'CMCU4557748' or 'SEKU9206534'."
      }
    },
    "additionalProperties": false
  }
} 